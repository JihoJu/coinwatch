# .env.example
# 이 파일을 .env 로 복사하여 실제 환경 변수 값을 설정하세요.

# 로깅 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# 기본값: INFO
LOG_LEVEL=INFO

# Kafka 브로커 주소 (쉼표로 구분)
# 기본값: localhost:9092
KAFKA_BROKERS=localhost:9092

# Kafka 토픽 이름
# 기본값: upbit-ticker-data
KAFKA_TOPIC=upbit-ticker-data

# Upbit 웹소켓 API URI
# 기본값: wss://api.upbit.com/websocket/v1
UPBIT_WEBSOCKET_URI=wss://api.upbit.com/websocket/v1

# 수집할 마켓 코드 (쉼표로 구분)
# 예: KRW-BTC,KRW-ETH
# 기본값: KRW-BTC
MARKET_CODES=KRW-BTC